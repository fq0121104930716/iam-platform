# ============================================================
# IAM Gateway - Development Environment Configuration
# ============================================================

# Logging dev settings
logging:
  level:
    root: INFO
    iam.platform.gateway: DEBUG
    org.springframework.security: DEBUG
    org.springframework.cloud.gateway: TRACE
