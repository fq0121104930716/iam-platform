# ============================================================
# IAM Audit Server - Development Environment Configuration
# ============================================================

# Logging dev settings
logging:
  level:
    iam.platform.audit: DEBUG
    org.apache.rocketmq: DEBUG
